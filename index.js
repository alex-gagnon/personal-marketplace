const path = require('path');

/** Absolute path to the skills directory. Use as Claude Code's `skillsDirectory`. */
const skillsDir = path.join(__dirname, 'skills');

module.exports = { skillsDir };
